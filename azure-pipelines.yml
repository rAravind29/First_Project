# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

stages:
  - stage: 'Unit_1'
    displayName: 'Unit Staging 1'
    jobs:
      - job: 'Unit_Job'
        displayName: 'Running Unit Job'
        steps:
          - script: echo 'Running job from azure through Git'

